import type { Metadata } from "next";

export const DEFAULT_SEO: Metadata = {
    openGraph: {
        type: "article",
        locale: "id_ID",
        siteName: "Wiatour",
    },
    twitter: {
        card: "summary_large_image",
    },
};
